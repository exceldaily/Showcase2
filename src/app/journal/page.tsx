import JournalView from "@/components/journal/JournalView";

export const dynamic = "force-dynamic";
export const metadata = { title: "Journal — AlphaForge" };

export default function JournalPage() {
  return <JournalView />;
}
